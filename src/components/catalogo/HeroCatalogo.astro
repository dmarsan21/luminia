---
import { Image } from "astro:assets";
import heroImage from "../../assets/images/hero.png";
---

<section
  class="relative h-[80vh] w-full flex items-center justify-center overflow-hidden bg-black"
>
  <!-- Imagen de fondo -->
  <div class="absolute inset-0 z-0">
    <Image
      src={heroImage}
      alt="Catálogo de Velas Hero"
      class="w-full h-full object-cover opacity-50"
      quality="max"
      loading="eager"
    />
    <div
      class="absolute inset-0 bg-linear-to-b from-black/40 via-transparent to-black/40"
    >
    </div>
  </div>

  <!-- Contenido centrado -->
  <div class="relative z-10 w-full max-w-3xl mx-auto px-6 text-center">
    <span
      class="text-base md:text-xl uppercase tracking-[0.5em] text-white mb-8 block font-medium"
    >
      Catálogo
    </span>

    <h1
      class="text-5xl md:text-7xl font-serif leading-tight mb-12 tracking-tight drop-shadow-2xl"
    >
      <span class="text-[#FFD700]">Velas que</span>
      <span class="italic font-light text-white">iluminan tu esencia</span>
    </h1>

    <div class="w-36 h-0.5 bg-white mx-auto mb-12"></div>

    <p
      class="text-lg md:text-2xl text-gray-200 max-w-3xl mx-auto font-light leading-relaxed drop-shadow-xl"
    >
      Descubre nuestra colección de velas aromáticas y encuentra tu aroma ideal.
    </p>
  </div>
</section>

<style>
  /* Refuerzo de contraste para mantener consistencia con el Hero de 'nosotros' */
  h1 {
    filter: drop-shadow(0 0 30px rgba(0, 0, 0, 0.8));
    letter-spacing: -0.02em;
  }
  p {
    filter: drop-shadow(0 0 20px rgba(0, 0, 0, 0.8));
  }
</style>
